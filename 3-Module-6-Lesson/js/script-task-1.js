// Destruction objects

const account = {
  balance: 100,
  credit: 50,
  bonus: 75,
};

const getFullBalance = account => {};

// Destruction array

const salaries = [90, 100, 110]; // low mid hight
