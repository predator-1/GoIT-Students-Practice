/**
 * Create team
 */

const roles = {
  FRONTEND: 'front-end',
  BACKEND: 'back-end',
  PM: 'project manager',
};

const members = {}; // each member has 'name', 'salary', 'role'

const createMember = () => {}; //factory
const addMember = () => {};
const addMembers = () => {};
const removeMemberByName = () => {};
const getMembersCount = () => {};
const getTeamCost = () => {}; // all members salaries
const getAvarageSalary = () => {};
